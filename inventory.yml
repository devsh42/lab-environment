all:
  children:
    splunk_servers:
      hosts:
        splunk-server:
          ansible_host: splunk
          ansible_user: root
    cribl_servers:
      hosts:
        cribl-server:
          ansible_host: cribl
          ansible_user: root

