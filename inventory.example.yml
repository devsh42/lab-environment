all:
  children:
    splunk_servers:
      hosts:
        splunk-server:
          ansible_host: splunk  # Update with your Splunk server IP
          ansible_user: root    # Use root or existing user with sudo access
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Optional: specify SSH key
    cribl_servers:
      hosts:
        cribl-server:
          ansible_host: splunk  # Update with your Cribl server IP
          ansible_user: root    # Use root or existing user with sudo access
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Optional: specify SSH key

